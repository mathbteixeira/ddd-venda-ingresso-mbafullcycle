package com.fullcycle.events.domain.repositories;

import com.fullcycle.domain.repository.IRepository;
import com.fullcycle.events.domain.entities.Partner;

public interface IPartnerRepository extends IRepository<Partner> {
    // métodos específicos de Partner podem ser adicionados aqui
}